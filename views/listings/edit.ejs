<% layout("layouts/boilerplate.ejs") %>
<div class="container my-5">
  <h2 class="text-center mb-4 fw-bold text-primary">Edit Your Listing</h2>

  <form action="/listings/<%= listing._id %>?_method=PUT" method="POST" class="p-4 shadow rounded bg-white needs-validation" novalidate enctype="multipart/form-data">

    <!-- Row 1: Title + Image Upload -->
    <div class="row g-3 mb-4">
      <div class="col-md-6">
        <label for="title" class="form-label">Title</label>
        <input type="text" class="form-control" id="title" name="title" value="<%= listing.title %>" required>
        <div class="invalid-feedback">Please provide a valid title.</div>
      </div>
      <div class="col-md-6">
        <label for="image" class="form-label">Upload Image</label>
        <input type="file" class="form-control" id="image" name="image">
        <% if (listing.image && listing.image.url) { %>
          <div class="mt-3">
            <p class="mb-1 fw-semibold">Current Image:</p>
            <img src="<%= orginalUrl %>" alt="Current Image" class="img-fluid rounded shadow-sm border" style="max-height: 200px;">
          </div>
        <% } %>
      </div>
    </div>

    <!-- Description -->
    <div class="mb-4">
      <label for="description" class="form-label">Description</label>
      <textarea class="form-control" id="description" name="description" rows="3" required><%= listing.description %></textarea>
      <div class="invalid-feedback">Provide a short description.</div>
    </div>

    <!-- Price, Location, Country, View Point -->
    <div class="row g-3 mb-4">
      <div class="col-md-6 col-lg-3">
        <label for="price" class="form-label">Price (₹)</label>
        <input type="number" class="form-control" id="price" name="price" value="<%= listing.price %>" required>
        <div class="invalid-feedback">Please provide a valid price.</div>
      </div>

      <div class="col-md-6 col-lg-3">
        <label for="location" class="form-label">Location</label>
        <input type="text" class="form-control" id="location" name="location" value="<%= listing.location %>" required>
        <div class="invalid-feedback">Please provide a valid location.</div>
      </div>

      <div class="col-md-6 col-lg-3">
        <label for="country" class="form-label">Country</label>
        <select class="form-select" id="country" name="country" required>
          <option value="" disabled <%= !listing.country ? 'selected' : '' %>>Select Country</option>
          <option value="India" <%= listing.country === 'India' ? 'selected' : '' %>>India</option>
          <option value="Pakistan" <%= listing.country === 'Pakistan' ? 'selected' : '' %>>Pakistan</option>
    <option value="Bangladesh" <%= listing.country === 'Bangladesh' ? 'selected' : '' %>>Bangladesh</option>
    <option value="Sri Lanka" <%= listing.country === 'Sri Lanka' ? 'selected' : '' %>>Sri Lanka</option>
    <option value="Nepal" <%= listing.country === 'Nepal' ? 'selected' : '' %>>Nepal</option>
    <option value="Bhutan" <%= listing.country === 'Bhutan' ? 'selected' : '' %>>Bhutan</option>
    <option value="Maldives" <%= listing.country === 'Maldives' ? 'selected' : '' %>>Maldives</option>
    <option value="Afghanistan" <%= listing.country === 'Afghanistan' ? 'selected' : '' %>>Afghanistan</option>
    <option value="China" <%= listing.country === 'China' ? 'selected' : '' %>>China</option>
    <option value="Japan" <%= listing.country === 'Japan' ? 'selected' : '' %>>Japan</option>
    <option value="South Korea" <%= listing.country === 'South Korea' ? 'selected' : '' %>>South Korea</option>
    <option value="Thailand" <%= listing.country === 'Thailand' ? 'selected' : '' %>>Thailand</option>
    <option value="Malaysia" <%= listing.country === 'Malaysia' ? 'selected' : '' %>>Malaysia</option>
    <option value="Singapore" <%= listing.country === 'Singapore' ? 'selected' : '' %>>Singapore</option>
    <option value="Indonesia" <%= listing.country === 'Indonesia' ? 'selected' : '' %>>Indonesia</option>
    <option value="Vietnam" <%= listing.country === 'Vietnam' ? 'selected' : '' %>>Vietnam</option>
    <option value="United States" <%= listing.country === 'United States' ? 'selected' : '' %>>United States</option>
    <option value="Canada" <%= listing.country === 'Canada' ? 'selected' : '' %>>Canada</option>
    <option value="United Kingdom" <%= listing.country === 'United Kingdom' ? 'selected' : '' %>>United Kingdom</option>
    <option value="Germany" <%= listing.country === 'Germany' ? 'selected' : '' %>>Germany</option>
    <option value="France" <%= listing.country === 'France' ? 'selected' : '' %>>France</option>
    <option value="Italy" <%= listing.country === 'Italy' ? 'selected' : '' %>>Italy</option>
          <option value="Australia" <%= listing.country === 'Australia' ? 'selected' : '' %>>Australia</option>
          <option value="New Zealand" <%= listing.country === 'New Zealand' ? 'selected' : '' %>>New Zealand</option>
        </select>
        <div class="invalid-feedback">Please select a valid country.</div>
      </div>

      <div class="col-md-6 col-lg-3">
        <label for="viewPoint" class="form-label">View Point</label>
        <select class="form-select" id="viewPoint" name="viewPoint" required>
          <option value="" disabled <%= !listing.viewPoint ? 'selected' : '' %>>Select View Point</option>
          <option value="Cities" <%= listing.viewPoint === 'Cities' ? 'selected' : '' %>>Cities</option>
          <option value="Mountain Side" <%= listing.viewPoint === 'Mountain Side' ? 'selected' : '' %>>Mountain Side</option>
          <option value="In Water" <%= listing.viewPoint === 'In Water' ? 'selected' : '' %>>In Water</option>
          <option value="Castles" <%= listing.viewPoint === 'Castles' ? 'selected' : '' %>>Castles</option>
          <option value="Domes" <%= listing.viewPoint === 'Domes' ? 'selected' : '' %>>Domes</option>
          <option value="Arctic" <%= listing.viewPoint === 'Arctic' ? 'selected' : '' %>>Arctic</option>
          <option value="Campaign" <%= listing.viewPoint === 'Campaign' ? 'selected' : '' %>>Campaign</option>
          <option value="Most Visited" <%= listing.viewPoint === 'Most Visited' ? 'selected' : '' %>>Most Visited</option>
          <option value="Amazing Pools" <%= listing.viewPoint === 'Amazing Pools' ? 'selected' : '' %>>Amazing Pools</option>
        </select>
        <div class="invalid-feedback">Please select a valid view point.</div>
      </div>
    </div>

    <!-- Submit Button -->
    <div class="text-center mt-4">
      <button type="submit" class="btn btn-primary px-5 py-2 shadow-sm">
        <i class="fa-solid fa-circle-check me-2"></i>Update Listing
      </button>
    </div>

  </form>
</div>
<script>
      // Example starter JavaScript for disabling form submissions if there are invalid fields
(() => {
  'use strict'

  // Fetch all the forms we want to apply custom Bootstrap validation styles to
  const forms = document.querySelectorAll('.needs-validation')

  // Loop over them and prevent submission
  Array.from(forms).forEach(form => {
    form.addEventListener('submit', event => {
      if (!form.checkValidity()) {
        event.preventDefault()
        event.stopPropagation()
      }

      form.classList.add('was-validated')
    }, false)
  })
})()

</script>